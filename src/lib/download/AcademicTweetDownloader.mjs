"use strict";

class AcademicTweetDownloader {
	constructor() {
		this.endpoint = `https://example.com/api/`
		// TODO: Find a promise-based timer that we can control more precisely than setInterval & clearInterval.
	}
}

export default AcademicTweetDownloader;
